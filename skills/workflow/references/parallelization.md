# Parallelization Rules

並行化詳細規則已移至 `main` skill。

→ see `~/.claude/skills/main/references/parallelization.md`

## 快速參考

| 操作 | 處理 |
|------|------|
| 讀取多個不同檔案 | **並行** |
| 搜尋多個不同模式 | **並行** |
| 多個獨立 D→R→T | **並行啟動** |
| Read → Edit 同一檔案 | 串行 |

**完整規則請參考 main skill。**

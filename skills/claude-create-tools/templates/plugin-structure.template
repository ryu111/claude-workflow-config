{{plugin-name}}/
├── .claude-plugin/
│   └── plugin.json                 # 必須：插件清單
│
├── commands/                       # 快捷命令（Markdown）
│   └── {{command-name}}.md
│
├── agents/                         # 自定義 agents
│   └── {{agent-name}}.md
│
├── skills/                         # Skills
│   └── {{skill-name}}/
│       ├── SKILL.md
│       └── references/
│           └── {{reference}}.md
│
├── hooks/
│   └── hooks.json                  # Hook 配置
│
├── scripts/                        # 腳本
│   ├── {{script-name}}.sh
│   └── {{script-name}}.py
│
├── .mcp.json                       # MCP 配置（可選）
│   # {
│   #   "mcpServers": {
│   #     "{{server-name}}": {
│   #       "command": "${CLAUDE_PLUGIN_ROOT}/servers/{{server}}",
│   #       "args": []
│   #     }
│   #   }
│   # }
│
├── .lsp.json                       # LSP 配置（可選）
│   # {
│   #   "{{language}}": {
│   #     "command": "{{lsp-command}}",
│   #     "args": [],
│   #     "extensionToLanguage": {
│   #       ".{{ext}}": "{{language}}"
│   #     }
│   #   }
│   # }
│
└── README.md                       # 說明文檔（給人類）
